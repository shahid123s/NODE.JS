const data = require('../JSON importing/data.json') //we can remove the .json extention but if there any js file node.js will first check the js then only check for the json file 

console.log(data.name);
console.log("Shahid")